- name: Install ovn-tester
  hosts: tester_hosts
  tasks:
    - name: Pull latest containers
      shell: |
              set -e
              podman pull {{ registry_node }}:5000/ovn/ovn-tester
              podman tag {{ registry_node }}:5000/ovn/ovn-tester ovn/ovn-tester
